cd to backend and frontend install node modules in both
get the credentials to the .env file
keep .env inside backend/.env
open the frontend in terminal and run npm run dev
open the backend in integrated terminal and run npm run dev
